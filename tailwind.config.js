const { fontFamily } = require('tailwindcss/defaultTheme')

module.exports = {
  darkMode: 'class', // Important to enable dark mode via the 'dark' class
  theme: {
    extend: {
    },
  },
  plugins: [],
}